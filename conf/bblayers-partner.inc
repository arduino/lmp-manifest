# This is a FoundriesFactory Partner bblayers include file.
# The layers meta-arduino-nxp and meta-arduino-lmp are
# FoundriesFactory partner layers.
# Do not remove unless you really know what you are doing.
BASELAYERS += "${OEROOT}/layers/meta-arduino/meta-arduino-nxp"
BASELAYERS += "${OEROOT}/layers/meta-arduino/meta-arduino-lmp"
